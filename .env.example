# Application (for local/XAMPP: use APP_URL=http://localhost/khbeventsboothsystemkmall)
APP_NAME="KHB Booths Booking System"
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost

APP_KEY=

# Database (XAMPP default: root with no password)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=khbeventskmallxmas
DB_USERNAME=root
DB_PASSWORD=

# Optional: path to mysql/mysqldump (e.g. for XAMPP: /opt/lampp/bin)
# DB_MYSQL_PATH=/opt/lampp/bin

# Optional: direct push/pull to cPanel (see docs/XAMPP-SETUP.md)
# REMOTE_DB_HOST=your-server.com
# REMOTE_DB_PORT=3306
# REMOTE_DB_DATABASE=cpanel_db_name
# REMOTE_DB_USERNAME=cpanel_db_user
# REMOTE_DB_PASSWORD=cpanel_db_password
# For SSH-based push/pull (no need to open MySQL to the internet):
# REMOTE_SSH_HOST=your-server.com
# REMOTE_SSH_USER=cpanel_username

# GitHub Configuration
# Copy this file to .env and fill in your GitHub credentials

# GitHub Username
GITHUB_USERNAME=your_github_username

# GitHub Personal Access Token (PAT)
# Generate one at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos)
GITHUB_TOKEN=your_github_personal_access_token

# GitHub Repository URL (optional - will use remote if not set)
GITHUB_REPO_URL=https://github.com/chamnabmeyinfo/khbevents-boothsystem-kmall.git

# Use SSH instead of HTTPS (true/false)
# If true, you need to set up SSH keys instead of using token
GITHUB_USE_SSH=false
